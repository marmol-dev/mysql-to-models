{
  "name": "mysql-to-model",
  "version": "0.0.1",
  "description": "Generates a database JSON schema from an existing mysql database",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index"
  },
  "bin": {
    "mysqltomodels": "bin/mysqltomodels.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.16.6",
    "mysql": "^2.11.1",
    "pcre-to-regexp": "0.0.4",
    "reflect-metadata": "^0.1.8",
    "xserializer": "0.0.5"
  }
}
